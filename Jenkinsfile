pipeline {
  agent any
  stages { 
    stage("Prebuild") {
      steps {
        echo "Prebuild"
      }    
    }
    stage("Test") {
      steps {
        echo "test"
      }
    }
    stage("Postbuild") {
      steps {
        echo "Postbuild"
      }
    }
  }
}
